package main

import (
	"fmt"
)

func main() {
	//array
	as := [...]string{"a", "b", "c", "d", "ef", "g", "h", "j"}
	fmt.Println(as)
	fmt.Println(len(as))
	fmt.Printf("%T\n", as)

	// vs code shortcut
	// alt   click
	// alt+shift   drag
	// alt+shift+i  cursors at end of each line for selection

}
